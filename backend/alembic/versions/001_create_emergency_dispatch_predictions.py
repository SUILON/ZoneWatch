"""Create emergency_dispatch_predictions table

Revision ID: 001
Revises:
Create Date: 2025-09-26 10:00:00.000000

"""
from alembic import op
import sqlalchemy as sa
from sqlalchemy.dialects.postgresql import UUID


# revision identifiers, used by Alembic.
revision = '001'
down_revision = None
branch_labels = None
depends_on = None


def upgrade():
    # ### commands auto generated by Alembic - please adjust! ###
    op.create_table('emergency_dispatch_predictions',
    sa.Column('id', UUID(as_uuid=True), nullable=False),
    sa.Column('prediction_date', sa.Date(), nullable=False, comment='予測対象日'),
    sa.Column('predicted_count', sa.Integer(), nullable=False, comment='予測救急出場件数'),
    sa.Column('confidence_score', sa.Float(), nullable=True, comment='予測の信頼度スコア'),
    sa.Column('model_name', sa.String(length=100), nullable=False, comment='使用したモデル名'),
    sa.Column('model_version', sa.String(length=50), nullable=True, comment='モデルバージョン'),
    sa.Column('dagshub_run_id', sa.String(length=100), nullable=True, comment='DAGsHub実行ID'),
    sa.Column('created_at', sa.DateTime(), nullable=False, comment='予測実行日時'),
    sa.PrimaryKeyConstraint('id')
    )
    op.create_index(op.f('ix_emergency_dispatch_predictions_prediction_date'), 'emergency_dispatch_predictions', ['prediction_date'], unique=False)
    # ### end Alembic commands ###


def downgrade():
    # ### commands auto generated by Alembic - please adjust! ###
    op.drop_index(op.f('ix_emergency_dispatch_predictions_prediction_date'), table_name='emergency_dispatch_predictions')
    op.drop_table('emergency_dispatch_predictions')
    # ### end Alembic commands ###