# =============================================================================
# Zone Watch - 環境変数設定ファイル
# =============================================================================
# このファイルをコピーして .env として使用してください
# 本番環境では適切なセキュアな値に変更してください

# =============================================================================
# データベース設定
# =============================================================================
# PostgreSQL設定
POSTGRES_USER=user
POSTGRES_PASSWORD=password
POSTGRES_DB=mydatabase
POSTGRES_HOST=postgres
POSTGRES_PORT=5432

# データベース接続URL（アプリケーション用）
DATABASE_URL=postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@${POSTGRES_HOST}:${POSTGRES_PORT}/${POSTGRES_DB}


# =============================================================================
# MLflow設定（DAGsHub）
# =============================================================================
# DAGsHubのMLflowトラッキングURI（実際のリポジトリに変更してください）
MLFLOW_TRACKING_URI=
# DAGsHubのユーザートークン（DAGsHub Settings > Tokens から取得）
DAGSHUB_USER_TOKEN=
# DAGsHubのトークンを利用したユーザー
DAGSHUB_USER_TOKEN=

# =============================================================================
# アプリケーション設定
# =============================================================================
# 環境（development, production, test）
ENVIRONMENT=development

# デバッグモード
DEBUG=true

# ログレベル（DEBUG, INFO, WARNING, ERROR, CRITICAL）
LOG_LEVEL=DEBUG

# =============================================================================
# セキュリティ設定
# =============================================================================
# JWT設定（本番では複雑な値に変更）
JWT_SECRET_KEY=your-secret-key-change-this-in-production
JWT_ALGORITHM=HS256
JWT_ACCESS_TOKEN_EXPIRE_MINUTES=30

# CORS設定
ALLOWED_HOSTS=localhost,127.0.0.1,0.0.0.0
CORS_ORIGINS=http://localhost:5173,http://127.0.0.1:5173

# =============================================================================
# Jupyter設定
# =============================================================================
JUPYTER_PORT=8888
JUPYTER_TOKEN=
NB_UID=1000
NB_GID=1000
CHOWN_HOME=yes
CHOWN_HOME_OPTS=-R

# =============================================================================
# Frontend設定（Vite）
# =============================================================================
VITE_API_BASE_URL=http://localhost:8000
VITE_MLFLOW_TRACKING_URI=https://dagshub.com/tsubasa-km/zone-watch.mlflow
VITE_APP_TITLE=Zone Watch
VITE_APP_VERSION=1.0.0

# =============================================================================
# Backend設定
# =============================================================================
BACKEND_HOST=0.0.0.0
BACKEND_PORT=8000

# =============================================================================
# 外部API設定（必要に応じて）
# =============================================================================
# EXTERNAL_API_KEY=your-api-key
# EXTERNAL_API_URL=https://api.example.com